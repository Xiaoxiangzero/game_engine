/**
 * Mini Game Engine - Audio Module Index
 * 音频模块索引文件
 */

export { AudioClip, AudioSource, AudioListener, AudioManager } from './AudioManager.js';

import { AudioClip, AudioSource, AudioListener, AudioManager } from './AudioManager.js';

export default {
    AudioClip,
    AudioSource,
    AudioListener,
    AudioManager
};

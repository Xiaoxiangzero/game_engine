/**
 * Mini Game Engine - Resources Module Index
 * 资源模块索引文件
 */

export { ResourceManager, ResourceCache, LoadableResource, ModelLoader } from './ResourceManager.js';

import { ResourceManager, ResourceCache, LoadableResource, ModelLoader } from './ResourceManager.js';

export default {
    ResourceManager,
    ResourceCache,
    LoadableResource,
    ModelLoader
};

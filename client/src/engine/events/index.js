/**
 * Mini Game Engine - Events Module Index
 * 事件模块索引文件
 */

export { GameEvent, EventListener, EventManager, Events, EventTypes } from './EventManager.js';

import { GameEvent, EventListener, EventManager, Events, EventTypes } from './EventManager.js';

export default {
    GameEvent,
    EventListener,
    EventManager,
    Events,
    EventTypes
};
